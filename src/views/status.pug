extends ./extends/layout

block variables
	- var title = 'Cloudflare Detector'

block content
	.container
			.heading
				h2
					a(href='{{SITE_LINK}}') {{SITE}}
					| {{STATUS}}
			.center
				a(href='/') ⟵ Back
